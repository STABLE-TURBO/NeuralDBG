# DEPRECATED: This workflow has been consolidated into ci.yml
# This file is kept for reference only and will be removed in a future release.
# Please refer to ci.yml for the current CI/CD pipeline.

name: Python package (DEPRECATED)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️  This workflow is deprecated and has been consolidated into ci.yml"
          echo "Please use the main CI workflow instead."
          exit 1
